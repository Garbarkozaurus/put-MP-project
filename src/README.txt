blender --background --python render_blender.py -- <path to .off> --output_folder <desired output folder> --scale 0.01

Dla jednego obiektu, generuje 30 obrazków dookoła, zwraca rgb, depth i dwa inne. Generuje folder train w desired output folder i tam zapisuje pliki.
